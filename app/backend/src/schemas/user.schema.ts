// import * as zod from 'zod';

// const emailSchema = zod.record(zod.literal('email'), zod.string().email());
// const passSchema = zod.record(zod.literal('password'), zod.string().min(6));
// // const userSchema = zod.object({
// //   // email: zod.string().email(),
// //   emailSchema,
// //   password: zod.string().min(6),
// // });

// export { emailSchema, passSchema };
